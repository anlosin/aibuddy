apiVersion: v1
kind: Secret
metadata:
  name: my-app-secret
  namespace: production
  labels:
    app.kubernetes.io/name: my-app
type: Opaque
stringData:
  DATABASE_PASSWORD: "changeme"
  API_KEY: "secret-api-key"
# 生产环境严禁把明文 Secret 提交到 Git。
# 改用 Sealed Secrets / External Secrets Operator / Vault 注入。
